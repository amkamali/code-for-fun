[project]
name = "python_fun_projects"
version = "0.0.0"
description = "Mini projects in Python for fun"
authors = [{ name = "Amir Kamali" }]
requires-python = ">=3.14"
dependencies = [
    "pillow>=12.0.0",
    "pytesseract>=0.3.13",
]

[build_syetem]
requires = ["hatchling"]
build-backend = "hatchling.build" 
